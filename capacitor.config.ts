import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.apiCosumerIonic',
  appName: 'apiCosumerIonic',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
