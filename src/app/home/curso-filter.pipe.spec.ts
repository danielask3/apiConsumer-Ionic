import { CursoFilterPipe } from './curso-filter.pipe';

describe('CursoFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CursoFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
